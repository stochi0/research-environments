[project]
name = "rlm-skill-edit"
version = "0.1.0"
description = "Safe single-occurrence string replacement."
requires-python = ">=3.10"

[project.scripts]
edit = "edit.edit:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/edit"]
