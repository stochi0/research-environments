[project]
name = "opencode-science"
description = "Solve science problems using OpenCode agent"
tags = ["science", "opencode", "multi-turn"]
version = "0.1.1"
requires-python = ">=3.10"
dependencies = [
    "verifiers>=0.1.12.dev1",
    "math-verify>=0.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
