[project]
name = "science-env"
description = "A collection of challenging single-turn science problems"
tags = ["science", "single-turn"]
version = "0.1.3"
requires-python = ">=3.10"
dependencies = [
    "verifiers>=0.1.12.dev1",
    "math-verify>=0.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
